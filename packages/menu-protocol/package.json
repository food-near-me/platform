{
  "name": "@foodnearme/menu-protocol",
  "version": "0.1.0",
  "description": "Menu Protocol (MP) specification and Zod schemas",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "prepare": "tsc",
    "dev": "tsc -w",
    "test": "node --import tsx --test src/*.test.ts"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4.20.0",
    "typescript": "^5.4.5"
  }
}
